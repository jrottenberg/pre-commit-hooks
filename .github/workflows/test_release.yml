name: Test Release Actions
on:
  push:
    branches-ignore:
      - main
jobs:
  release-test:
    runs-on: ubuntu-latest
    container: node:lts
    steps:
      - uses: actions/checkout@v3
      - run: npm install @semantic-release/github && npx semantic-release --dry-run --no-ci --branches $CI_COMMIT_BRANCH
